import type { ProductTypes } from 'vtex.product-context';
export declare const DEFAULT_WIDTH = "auto";
export declare const DEFAULT_HEIGHT = "auto";
export declare const MAX_WIDTH = 3000;
export declare const MAX_HEIGHT = 4000;
export declare type PreferenceType = 'FIRST_AVAILABLE' | 'LAST_AVAILABLE' | 'PRICE_ASC' | 'PRICE_DESC';
export declare function cleanImageUrl(imageUrl: string): {
    cleanUrl: string;
    vParam: string;
} | {
    cleanUrl: string;
    vParam?: undefined;
};
export declare function changeImageUrlSize(imageUrl: string, width?: string | number, height?: string | number): string | undefined;
/**
 * @description
 * This method is responsible for constructing the product type for the product-context.
 * The property `product.sku` is built by these specifications
 * */
export declare function mapCatalogProductToProductSummary(product: ProductTypes.Product, preferredSKU?: PreferenceType, imageSize?: string | number): any;
