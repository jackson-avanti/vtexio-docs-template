import { FC, ReactNode } from 'react';
import { SliderLayoutProps, SliderLayoutSiteEditorProps } from './SliderContext';
interface Props extends SliderLayoutSiteEditorProps {
    arrowSize: number;
    totalItems: number;
    itemsPerPage: number;
    centerMode: SliderLayoutProps['centerMode'];
    centerModeSlidesGap?: SliderLayoutProps['centerModeSlidesGap'];
    children?: Array<Exclude<ReactNode, boolean | null | undefined>>;
}
export declare const CSS_HANDLES: readonly ["sliderLayoutContainer", "sliderTrackContainer", "sliderTrack", "slide", "slideChildrenContainer", "sliderLeftArrow", "sliderRightArrow", "sliderArrows", "paginationDotsContainer", "paginationDot"];
declare const Slider: FC<Props>;
export default Slider;
