import { FC, ReactNode } from 'react';
import { SliderLayoutProps } from './SliderContext';
export declare const CSS_HANDLES: readonly ["sliderTrack", "slide", "slideChildrenContainer"];
interface Props {
    totalItems: number;
    infinite: boolean;
    usePagination: boolean;
    centerMode: SliderLayoutProps['centerMode'];
    centerModeSlidesGap?: SliderLayoutProps['centerModeSlidesGap'];
    children?: Array<Exclude<ReactNode, boolean | null | undefined>>;
}
declare const SliderTrack: FC<Props>;
export default SliderTrack;
