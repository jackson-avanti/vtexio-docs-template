import { SliderLayoutProps } from '../components/SliderContext';
export declare const useSliderVisibility: ({ currentSlide, slidesPerPage, totalItems, centerMode, }: {
    currentSlide: number;
    slidesPerPage: number;
    totalItems: number;
    centerMode: SliderLayoutProps['centerMode'];
}) => {
    shouldRenderItem: (index: number) => boolean;
    isItemVisible: (index: number) => boolean;
};
