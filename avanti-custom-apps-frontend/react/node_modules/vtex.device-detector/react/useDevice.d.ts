import type { RenderContext } from 'vtex.render-runtime';
declare type Device = RenderContext.RenderContext['deviceInfo']['type'];
export interface DeviceInfo {
    device: Device;
    isMobile: boolean;
}
declare const useDevice: () => {
    device: import("vtex.render-runtime/react/utils/withDevice").Device;
    isMobile: boolean;
};
export default useDevice;
