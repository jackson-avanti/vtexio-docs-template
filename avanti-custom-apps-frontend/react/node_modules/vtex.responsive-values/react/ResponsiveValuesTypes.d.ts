/** The user is able to input `mobile` as a device (as opposed to `desktop`),
 * but the output will be converted to `phone` and `tablet` */
export declare type InputDevice = 'mobile' | 'phone' | 'tablet' | 'desktop';
export declare type OutputDevice = 'phone' | 'tablet' | 'desktop';
export declare type ResponsiveInput<T> = Record<InputDevice | string, T>;
export declare type ResponsiveValue<T> = T | ResponsiveInput<T>;
export declare type ResponsiveValues = Record<string, ResponsiveValue<unknown>>;
export declare type GetResponsiveValueType<T extends ResponsiveValue<unknown>> = T extends ResponsiveValue<infer U> ? U : never;
