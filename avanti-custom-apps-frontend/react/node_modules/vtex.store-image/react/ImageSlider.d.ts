import type { SliderConfig, ImagesSchema } from './ImageTypes';
export interface ImageSliderProps {
    images: ImagesSchema;
    height: number;
    sliderLayoutConfig: SliderConfig;
    preload?: boolean;
}
declare function ImageSlider(props: ImageSliderProps): JSX.Element;
declare namespace ImageSlider {
    var schema: {
        title: string;
        description: string;
        type: string;
        properties: {
            height: {
                default: number;
                enum: number[];
                isLayout: boolean;
                title: string;
                type: string;
            };
            sliderLayoutConfig: {
                title: string;
                properties: {
                    infinite: {
                        default: boolean;
                        title: string;
                        type: string;
                    };
                    showNavigationArrows: {
                        default: string;
                        enum: string[];
                        title: string;
                        type: string;
                    };
                    showPaginationDots: {
                        default: string;
                        enum: string[];
                        title: string;
                        type: string;
                    };
                    usePagination: {
                        default: boolean;
                        title: string;
                        type: string;
                    };
                };
                type: string;
            };
        };
    };
}
export default ImageSlider;
