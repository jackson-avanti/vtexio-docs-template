import { FC } from 'react';
import { MessageDescriptor } from 'react-intl';
import { Values } from './formatIOMessage';
declare type IOMessageType = FC<MessageDescriptor & {
    values?: Values;
}>;
declare const IOMessage: IOMessageType;
export default IOMessage;
