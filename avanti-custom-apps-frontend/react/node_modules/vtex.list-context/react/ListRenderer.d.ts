import { CssHandlesTypes } from 'vtex.css-handles';
declare const CSS_HANDLES: readonly ["list", "item"];
interface Props {
    listElement?: string;
    itemElement?: string;
    classes?: CssHandlesTypes.CustomClasses<typeof CSS_HANDLES>;
}
declare function RenderList({ listElement, itemElement, classes, }: Props): JSX.Element;
export default RenderList;
