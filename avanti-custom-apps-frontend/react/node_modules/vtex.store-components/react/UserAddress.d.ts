/// <reference types="react" />
import type { CssHandlesTypes } from 'vtex.css-handles';
export declare const USER_ADDRESS_CSS_HANDLES: readonly ["userAddressContainer", "addressInfoIconContainer", "addressInfoTextContainer", "addressInfoPrefixContainer", "addressInfoAddressContainer", "addressInfoDivider", "addressInfoModalContainer"];
declare type Props = {
    /** Define if address should be displayed in a single line or in blocks */
    variation: 'inline' | 'bar';
    /** Define if street should be displayed */
    showStreet?: boolean;
    /** Define if city and state should be displayed */
    showCityAndState?: boolean;
    /** Define if postal code should be displayed */
    showPostalCode?: boolean;
    /** Define if prefix should be displayed */
    showPrefix?: boolean;
    /** Define if component should render if address is empty */
    showIfEmpty?: boolean;
    /** Define if component should render if ZipCode contains asterics */
    showIfMasked?: boolean;
    /** Used to override default CSS handles */
    classes?: CssHandlesTypes.CustomClasses<typeof USER_ADDRESS_CSS_HANDLES>;
};
declare function UserAddress({ variation, showStreet, showCityAndState, showPostalCode, showPrefix, showIfEmpty, showIfMasked, classes, }: Props): JSX.Element | null;
export default UserAddress;
