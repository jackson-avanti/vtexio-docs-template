import React from 'react';
import { Modal } from 'vtex.modal-layout';
interface Props {
    children: React.ReactNode;
    ModalZoomElement: typeof Modal;
}
declare function ModalZoom(props: Props): JSX.Element;
export default ModalZoom;
