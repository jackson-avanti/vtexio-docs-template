import { FC } from 'react';
import { Modal } from 'vtex.modal-layout';
import { ZoomMode } from './Zoomable';
interface Props {
    index: number;
    src: string;
    alt: string;
    imageLabel?: string;
    zoomMode: ZoomMode;
    zoomFactor: number;
    aspectRatio?: AspectRatio;
    maxHeight?: number | string;
    ModalZoomElement?: typeof Modal;
}
declare type AspectRatio = string | number;
declare const ProductImage: FC<Props>;
export default ProductImage;
