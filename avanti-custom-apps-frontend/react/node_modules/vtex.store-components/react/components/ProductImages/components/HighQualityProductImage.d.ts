/// <reference types="react" />
import { ZoomMode as ZoomModeComplete } from './Zoomable';
declare type AspectRatio = string | number;
declare type ZoomMode = Exclude<ZoomModeComplete, 'open-modal'>;
interface Props {
    zoomMode?: ZoomMode;
    zoomFactor?: number;
    aspectRatio?: AspectRatio;
    imageSizes?: number[];
    defaultSize?: number;
    maxSize?: number;
}
declare function HighQualityProductImage(props: Props): JSX.Element | null;
export default HighQualityProductImage;
