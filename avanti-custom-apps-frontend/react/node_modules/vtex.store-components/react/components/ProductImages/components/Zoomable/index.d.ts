import { FC, ReactElement } from 'react';
import { Modal } from 'vtex.modal-layout';
export declare type ZoomMode = 'in-place-click' | 'in-place-hover' | 'disabled' | 'open-modal';
interface Props {
    mode?: ZoomMode;
    zoomContent?: ReactElement;
    factor?: number;
    ModalZoomElement?: typeof Modal;
}
declare const Zoomable: FC<Props>;
export default Zoomable;
