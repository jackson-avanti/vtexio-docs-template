import { FC, ReactElement } from 'react';
declare type ZoomType = 'click' | 'hover';
interface Props {
    type: ZoomType;
    zoomContent?: ReactElement;
    factor: number;
}
declare const ZoomInPlace: FC<Props>;
export default ZoomInPlace;
