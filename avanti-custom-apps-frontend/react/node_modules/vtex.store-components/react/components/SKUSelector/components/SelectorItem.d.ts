import React, { SyntheticEvent } from 'react';
interface Props {
    isAvailable: boolean;
    isSelected: boolean;
    maxPrice?: number | null;
    price?: number | null;
    onClick: (e: SyntheticEvent<HTMLDivElement>) => void;
    isImage: boolean;
    variationValue: string;
    variationValueOriginalName: string;
    imageUrl?: string;
    imageLabel?: string | null;
    isImpossible: boolean;
    imageHeight?: number | string;
    imageWidth?: number | string;
    showBorders?: boolean;
    variationLabel: string;
    label: string;
}
export declare const CSS_HANDLES: readonly ["frameAround", "valueWrapper", "diagonalCross", "unavailable", "skuSelectorItem", "skuSelectorBadge", "skuSelectorItemImage", "skuSelectorInternalBox", "skuSelectorItemTextValue", "skuSelectorItemImageValue"];
/**
 * Inherits the components that should be displayed inside the Selector component.
 */
declare function SelectorItem({ isAvailable, isSelected, maxPrice, price, onClick, isImage, variationValue, variationValueOriginalName, imageUrl, imageLabel, isImpossible, imageHeight, imageWidth, showBorders, variationLabel, label, }: Props): JSX.Element;
declare const _default: React.MemoExoticComponent<typeof SelectorItem>;
export default _default;
