import React from 'react';
import type { CssHandlesTypes } from 'vtex.css-handles';
declare const CSS_HANDLES: readonly ["notificationContent"];
interface Props {
    /** Text to be used in the bar */
    content?: string;
    /** Used to override default CSS handles */
    classes?: CssHandlesTypes.CustomClasses<typeof CSS_HANDLES>;
}
declare function NotificationInline({ content, classes }: Props): JSX.Element | null;
declare const MemoizedNotificationInline: React.MemoExoticComponent<typeof NotificationInline> & {
    schema?: Record<string, string>;
};
export default MemoizedNotificationInline;
